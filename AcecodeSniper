-- KILLERHUBCODESNIPER | Final Loader
-- Discord: https://discord.gg/yvZXkAGKR

local discordLink = "https://discord.gg/yvZXkAGKR"
local logoId = "rbxassetid://110381055574517"

if game.CoreGui:FindFirstChild("KILLERHUB") then
    game.CoreGui:FindFirstChild("KILLERHUB"):Destroy()
end

local TweenService = game:GetService("TweenService")
local ScreenGui = Instance.new("ScreenGui", game.CoreGui)
ScreenGui.Name = "KILLERHUB"

-- INTRO
local Intro = Instance.new("Frame", ScreenGui)
Intro.Size = UDim2.new(1,0,1,0)
Intro.BackgroundColor3 = Color3.fromRGB(5,5,5)

local IntroLogo = Instance.new("ImageLabel", Intro)
IntroLogo.Size = UDim2.new(0,0,0,0)
IntroLogo.Position = UDim2.new(0.5,0,0.5,0)
IntroLogo.AnchorPoint = Vector2.new(0.5,0.5)
IntroLogo.BackgroundTransparency = 1
IntroLogo.Image = logoId

local IntroText = Instance.new("TextLabel", Intro)
IntroText.Size = UDim2.new(1,0,0,30)
IntroText.Position = UDim2.new(0,0,0.5,60)
IntroText.BackgroundTransparency = 1
IntroText.Text = "KILLERHUBCODESNIPER"
IntroText.TextColor3 = Color3.fromRGB(255,0,0)
IntroText.Font = Enum.Font.GothamBlack
IntroText.TextSize = 0
IntroText.TextTransparency = 1

TweenService:Create(IntroLogo, TweenInfo.new(0.6, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Size = UDim2.new(0,120,0,120)}):Play()
TweenService:Create(IntroText, TweenInfo.new(0.5, Enum.EasingStyle.Quad, Enum.EasingDirection.Out, 0, false, 0.4), {TextSize = 22, TextTransparency = 0}):Play()
task.wait(2)
Intro:Destroy()

-- MAIN GUI
local Main = Instance.new("Frame", ScreenGui)
Main.Size = UDim2.new(0, 360, 0, 350)
Main.Position = UDim2.new(0.5, -180, 0.5, -175)
Main.BackgroundColor3 = Color3.fromRGB(14,14,14)
Main.Active = true
Main.Draggable = true
Instance.new("UICorner", Main).CornerRadius = UDim.new(0, 16)

local Glow = Instance.new("UIStroke", Main)
Glow.Thickness = 3
Glow.Color = Color3.fromRGB(255,0,0)

local OuterGlow = Instance.new("Frame", Main)
OuterGlow.Size = UDim2.new(1, 20, 1, 20)
OuterGlow.Position = UDim2.new(0, -10, 0, -10)
OuterGlow.BackgroundTransparency = 1
OuterGlow.ZIndex = 0
local OuterStroke = Instance.new("UIStroke", OuterGlow)
OuterStroke.Thickness = 20
OuterStroke.Color = Color3.fromRGB(255,0,0)
OuterStroke.Transparency = 0.85
Instance.new("UICorner", OuterGlow).CornerRadius = UDim.new(0, 20)

task.spawn(function()
    while Main.Parent do
        TweenService:Create(Glow, TweenInfo.new(1, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {Thickness = 4, Transparency = 0}):Play()
        task.wait(1)
        TweenService:Create(Glow, TweenInfo.new(1, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {Thickness = 2, Transparency = 0.3}):Play()
        task.wait(1)
    end
end)

local Logo = Instance.new("ImageLabel", Main)
Logo.Size = UDim2.new(0, 80, 0, 80)
Logo.Position = UDim2.new(0.5, -40, 0, 15)
Logo.BackgroundTransparency = 1
Logo.Image = logoId

local Title = Instance.new("TextLabel", Main)
Title.Size = UDim2.new(1,0,0,25)
Title.Position = UDim2.new(0,0,0,100)
Title.BackgroundTransparency = 1
Title.Text = "KILLERHUBCODESNIPER"
Title.TextColor3 = Color3.new(1,1,1)
Title.Font = Enum.Font.GothamBlack
Title.TextSize = 18

-- UPDATED TEXT - No free mention
local Info = Instance.new("TextLabel", Main)
Info.Size = UDim2.new(1, -30, 0, 80)
Info.Position = UDim2.new(0, 15, 0, 130)
Info.BackgroundTransparency = 1
Info.Text = "⚠️ SCRIPT LOCKED\nTo get full access, you must join our Discord server.\n\n1. COPY the link below\n2. JOIN the server to get the script"
Info.TextColor3 = Color3.fromRGB(200,200,200)
Info.Font = Enum.Font.Gotham
Info.TextSize = 12
Info.TextWrapped = true
Info.TextYAlignment = Enum.TextYAlignment.Top

local LinkBox = Instance.new("TextLabel", Main)
LinkBox.Size = UDim2.new(0,320,0,36)
LinkBox.Position = UDim2.new(0,20,0,215)
LinkBox.BackgroundColor3 = Color3.fromRGB(22,22,22)
LinkBox.Text = "  "..discordLink
LinkBox.TextColor3 = Color3.fromRGB(88,101,242)
LinkBox.Font = Enum.Font.Code
LinkBox.TextSize = 12
LinkBox.TextXAlignment = Enum.TextXAlignment.Left
Instance.new("UICorner", LinkBox).CornerRadius = UDim.new(0, 8)

local CopyBtn = Instance.new("TextButton", Main)
CopyBtn.Size = UDim2.new(0,320,0,45)
CopyBtn.Position = UDim2.new(0,20,0,265)
CopyBtn.BackgroundColor3 = Color3.fromRGB(255,0,0)
CopyBtn.Text = "📋 COPY & JOIN DISCORD"
CopyBtn.TextColor3 = Color3.new(1,1,1)
CopyBtn.Font = Enum.Font.GothamBold
CopyBtn.TextSize = 14
Instance.new("UICorner", CopyBtn).CornerRadius = UDim.new(0, 8)

local Close = Instance.new("TextButton", Main)
Close.Size = UDim2.new(0,30,0,30)
Close.Position = UDim2.new(1,-35,0,8)
Close.BackgroundTransparency = 1
Close.Text = "X"
Close.TextColor3 = Color3.fromRGB(80,80,80)
Close.Font = Enum.Font.GothamBold
Close.TextSize = 18
Close.MouseButton1Click:Connect(function() ScreenGui:Destroy() end)

CopyBtn.MouseButton1Click:Connect(function()
    if setclipboard then setclipboard(discordLink) elseif toclipboard then toclipboard(discordLink) end
    CopyBtn.Text = "✅ COPIED! NOW JOIN"
    CopyBtn.BackgroundColor3 = Color3.fromRGB(0,170,0)
    task.wait(2)
    CopyBtn.Text = "📋 COPY & JOIN DISCORD"
    CopyBtn.BackgroundColor3 = Color3.fromRGB(255,0,0)
end)

Main.Size = UDim2.new(0,0,0,0)
TweenService:Create(Main, TweenInfo.new(0.5, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Size = UDim2.new(0,360,0,350)}):Play()
